import React from "react";
import { ProfileLayout, ProfilePolicyList } from "../../Components";

export const ProfilePrivacyScreen = () => {
  return (
    <ProfileLayout header="Profile and privacy">
      <ProfilePolicyList />
    </ProfileLayout>
  );
};
